---
interface Props {
	sectionClass?: string;
	containerClass?: string;
}

const {
	sectionClass = "",
	containerClass = "",
} = Astro.props;
---

<section class={sectionClass}>
	<div class={`flex flex-col md:flex-row 2xlp:border-x ${containerClass}`}>
		<slot />
	</div>
</section>
